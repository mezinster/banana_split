// Widget smoke tests for BananaSplitApp are covered by integration tests.
// This file is intentionally left minimal to avoid requiring full app bootstrap
// (asset loading, providers) in unit test context.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder: widget tests run', () {
    expect(true, isTrue);
  });
}
